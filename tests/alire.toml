name = "tests"
description = "Shiny new project"
version = "0.1.0-dev"

executables = ["tests"]

[[depends-on]]
adverserial = "*"

[[pins]]
adverserial = { path = ".." }

[build-profiles]
adverserial = "validation"
